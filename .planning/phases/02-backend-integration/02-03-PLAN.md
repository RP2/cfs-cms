---
phase: 02-backend-integration
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/routes/login/+page.svelte
  - src/routes/register/+page.svelte
  - src/routes/+layout.svelte
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users can register with email/password'
    - 'Users can login and stay logged in'
    - 'Form validation prevents invalid submissions'
    - 'UI matches existing design system'
  artifacts:
    - path: 'src/routes/login/+page.svelte'
      provides: 'User login interface'
      contains: 'form, email, password'
    - path: 'src/routes/register/+page.svelte'
      provides: 'User registration interface'
      contains: 'form, email, password, confirm'
    - path: 'src/routes/+layout.svelte'
      provides: 'Auth state handling'
      contains: 'auth store, redirect logic'
  key_links:
    - from: 'src/routes/login/+page.svelte'
      to: 'src/lib/auth.ts'
      via: 'form action'
      pattern: "auth\\.createSession"
    - from: 'src/routes/register/+page.svelte'
      to: 'src/lib/auth.ts'
      via: 'form action'
      pattern: "auth\\.createUser"
---

<objective>
Create login and register UI pages with form validation and auth integration.

Purpose: Provide user-friendly authentication interface matching existing design system.
Output: Working auth UI ready for backend integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/research/FEATURES.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
@src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Create login page</name>
  <files>src/routes/login/+page.svelte</files>
  <action>Create src/routes/login/+page.svelte with:
- Email and password form fields
- Form validation (required fields, email format)
- Submit handler calling Lucia auth
- Error handling and display
- Loading states during authentication
- Link to register page
- Style with existing shadcn components</action>
  <verify>Login page renders with form fields</verify>
  <done>Login page created with proper validation</done>
</task>

<task type="auto">
  <name>Create register page</name>
  <files>src/routes/register/+page.svelte</files>
  <action>Create src/routes/register/+page.svelte with:
- Email, password, and confirm password fields
- Form validation (password strength, matching confirm)
- Submit handler for user registration
- Error handling for duplicate emails
- Loading states and success feedback
- Link to login page
- Consistent styling with login page</action>
  <verify>Register page renders with all form fields</verify>
  <done>Registration page created with validation</done>
</task>

<task type="auto">
  <name>Add logout functionality</name>
  <files>src/routes/+layout.svelte</files>
  <action>Update root layout to handle authentication state:
- Add logout button to header/sidebar
- Implement logout action calling auth.invalidateSession
- Handle redirect to login after logout
- Show/hide UI elements based on auth state
- Preserve existing layout structure</action>
  <verify>Layout includes logout functionality</verify>
  <done>Logout functionality integrated into layout</done>
</task>

<task type="auto">
  <name>Add authentication routing</name>
  <files>src/routes/+layout.svelte</files>
  <action>Implement auth-based routing in root layout:
- Check auth state on page load
- Redirect unauthenticated users to /login
- Allow access to auth pages when not logged in
- Handle auth state changes gracefully
- Prevent flash of unauthenticated content</action>
  <verify>Unauthenticated users redirected to login</verify>
  <done>Authentication routing implemented</done>
</task>

</tasks>

<verification>
Test authentication UI:
- Login form accepts valid credentials
- Register form validates password requirements
- Logout button appears for authenticated users
- Unauthenticated users redirected to login
- Auth pages accessible when not logged in
</verification>

<success_criteria>

- Login page functional with email/password authentication
- Register page allows new user creation
- Form validation prevents invalid submissions
- Logout functionality works from any page
- Authentication routing prevents unauthorized access
- UI matches existing shadcn design system
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-03-SUMMARY.md`
</output>
