---
phase: 02-backend-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/auth.ts
  - prisma/schema.prisma
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users can create accounts with email and password'
    - 'Users can log in and remain authenticated across sessions'
    - 'User data is securely stored in database'
  artifacts:
    - path: 'src/lib/auth.ts'
      provides: 'Lucia authentication configuration'
      exports: 'auth'
    - path: 'prisma/schema.prisma'
      provides: 'Database schema with user/session tables'
      contains: 'model User, model UserSession'
  key_links:
    - from: 'src/lib/auth.ts'
      to: 'prisma/schema.prisma'
      via: 'Lucia SQLite adapter'
      pattern: 'sqlite.*db'
---

<objective>
Implement Lucia Auth foundation with database schema and server middleware.

Purpose: Establish self-hosted authentication system with database-backed sessions.
Output: Working auth infrastructure ready for UI integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/research/FEATURES.md
@.planning/research/STACK.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Install Lucia Auth dependencies</name>
  <files>package.json</files>
  <action>Install Lucia Auth and SQLite adapter:
- Run: npm install lucia @lucia-auth/adapter-sqlite
- Verify installation succeeds
- Update package.json with new dependencies</action>
  <verify>npm install completes without errors</verify>
  <done>Lucia Auth packages installed successfully</done>
</task>

<task type="auto">
  <name>Create Lucia auth configuration</name>
  <files>src/lib/auth.ts</files>
  <action>Create src/lib/auth.ts with Lucia configuration:
- Import lucia, sveltekit middleware, sqlite adapter
- Configure for Cloudflare D1 database
- Set session cookie options (no expiration)
- Export auth instance and types
- Follow Lucia v3 patterns from research</action>
  <verify>src/lib/auth.ts exports auth instance</verify>
  <done>Lucia auth configuration complete</done>
</task>

<task type="auto">
  <name>Update database schema for auth</name>
  <files>prisma/schema.prisma</files>
  <action>Add user and session tables to prisma/schema.prisma:
- Create User model with id, email, hashed_password, timestamps
- Create UserSession model with id, user_id, expires_at
- Add user_id columns to existing models (workspaces, files)
- Ensure SQLite compatibility for D1
- Follow schema patterns from PHASE2_ROADMAP.md</action>
  <verify>Schema contains User and UserSession models</verify>
  <done>Database schema updated for authentication</done>
</task>

<task type="auto">
  <name>Configure SvelteKit auth middleware</name>
  <files>src/hooks.server.ts</files>
  <action>Create/update src/hooks.server.ts with Lucia middleware:
- Import auth from lib/auth.ts
- Add auth.handleRequest() to hooks
- Configure for both API routes and page requests
- Handle session validation and user context</action>
  <verify>src/hooks.server.ts contains auth middleware</verify>
  <done>SvelteKit auth middleware configured</done>
</task>

<task type="auto">
  <name>Deploy database schema changes</name>
  <files>prisma/schema.prisma</files>
  <action>Apply database schema changes to D1:
- Run: npx wrangler d1 migrations create cfs_cms auth_tables
- Run: npx wrangler d1 migrations apply cfs_cms --local
- Verify tables created successfully</action>
  <verify>D1 database contains user and session tables</verify>
  <done>Database schema deployed to D1</done>
</task>

</tasks>

<verification>
Test auth foundation:
- Lucia auth instance initializes without errors
- Database schema deploys successfully
- User and session tables created
- Auth configuration ready for middleware
</verification>

<success_criteria>

- Lucia Auth installed and configured
- Database schema includes user/session tables
- D1 migrations applied successfully
- Auth foundation ready for SvelteKit integration
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-02-SUMMARY.md`
</output>
