---
phase: 02-backend-integration
plan: 06
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - src/hooks.server.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users can log in and remain authenticated across sessions'
    - 'User authentication state persists across page reloads'
    - 'Secure session management prevents unauthorized access'
  artifacts:
    - path: 'src/hooks.server.ts'
      provides: 'SvelteKit auth middleware'
      contains: 'auth.handleRequest'
  key_links:
    - from: 'src/hooks.server.ts'
      to: 'src/lib/auth.ts'
      via: 'handleRequest middleware'
      pattern: "auth\\.handleRequest"
---

<objective>
Implement SvelteKit auth middleware and deploy authentication infrastructure.

Purpose: Enable session-based authentication across the entire application.
Output: Working authentication middleware ready for UI integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/research/FEATURES.md
@.planning/research/STACK.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Configure SvelteKit auth middleware</name>
  <files>src/hooks.server.ts</files>
  <action>Create/update src/hooks.server.ts with Lucia middleware:
- Import auth from lib/auth.ts
- Add auth.handleRequest() to hooks
- Configure for both API routes and page requests
- Handle session validation and user context
- Enable secure session cookies</action>
  <verify>src/hooks.server.ts contains auth middleware</verify>
  <done>SvelteKit auth middleware configured</done>
</task>

<task type="auto">
  <name>Deploy authentication infrastructure</name>
  <files>src/hooks.server.ts</files>
  <action>Deploy middleware changes and verify auth setup:
- Run: npm run cf:deploy
- Verify middleware loads without errors
- Test basic auth functionality
- Confirm session management works</action>
  <verify>Auth middleware deployed successfully</verify>
  <done>Authentication infrastructure deployed</done>
</task>

</tasks>

<verification>
Test auth middleware:
- SvelteKit hooks load auth middleware without errors
- Session cookies work correctly
- Auth state persists across requests
- Middleware handles both API and page requests
</verification>

<success_criteria>

- SvelteKit middleware handles auth requests
- Session-based authentication functional
- Auth infrastructure deployed to Cloudflare
- Secure cookie management implemented
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-06-SUMMARY.md`
</output>
