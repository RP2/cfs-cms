---
phase: 02-backend-integration
plan: 05
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - src/lib/services/dataService.ts
  - src/lib/stores/index.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users see their personal files and workspaces immediately after login'
    - 'Changes made in the UI are automatically saved and synced'
    - 'Users cannot access other users' data'
  artifacts:
    - path: 'src/lib/services/dataService.ts'
      provides: 'Authenticated data operations'
      contains: 'auth.getUser'
    - path: 'src/lib/stores/index.ts'
      provides: 'User-aware state management'
      contains: 'currentUser'
  key_links:
    - from: 'src/lib/services/dataService.ts'
      to: 'src/lib/auth.ts'
      via: 'auth.getUser()'
      pattern: "const.*user.*=.*auth\\.getUser"
    - from: 'src/lib/stores/index.ts'
      to: 'src/lib/services/dataService.ts'
      via: 'user context'
      pattern: "currentUser.*\\$derived"
---

<objective>
Update dataService and stores to use authenticated user context instead of mock data.

Purpose: Complete the transition from mock data to real backend with user authentication.
Output: Fully integrated backend with preserved UI/UX experience.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
@src/lib/services/dataService.ts
@src/lib/stores/index.ts
</context>

<tasks>

<task type="auto">
  <name>Update dataService and stores for authenticated users</name>
  <files>src/lib/services/dataService.ts,src/lib/stores/index.ts</files>
  <action>Update dataService and stores to use authenticated user context:
- Add user parameter to all dataService functions
- Replace all 'user_1' references with user.id in workspace and file operations
- Update currentUser store with auth state and reactive updates
- Maintain optimistic UI updates with real persistence
- Ensure compatibility with existing components</action>
  <verify>dataService functions and stores use authenticated user</verify>
  <done>dataService and stores updated for authenticated users</done>
</task>

<task type="auto">
  <name>Add auth error handling</name>
  <files>src/lib/services/dataService.ts</files>
  <action>Add comprehensive error handling for auth scenarios:
- Handle session expiration gracefully
- Provide clear error messages for auth failures
- Maintain optimistic updates during auth errors
- Add retry logic for transient auth issues</action>
  <verify>Error handling covers auth failure scenarios</verify>
  <done>Auth error handling implemented</done>
</task>

</tasks>

<verification>
Test data service integration:
- All CRUD operations work with authenticated users
- UI updates immediately with backend persistence
- User data properly isolated between users
- Auth errors handled gracefully
- No breaking changes to component interfaces
</verification>

<success_criteria>

- All dataService functions use authenticated user context
- Hardcoded 'user_1' references completely removed
- Workspace operations tied to real users
- File operations respect user permissions
- Optimistic UI updates preserved with real backend
- Error handling for authentication failures
- No breaking changes to existing component interfaces
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-05-SUMMARY.md`
</output>
