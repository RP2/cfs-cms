---
phase: 02-backend-integration
plan: 07
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - src/routes/api/folders/+server.ts
  - src/routes/api/tags/+server.ts
  - src/routes/api/trash/+server.ts
  - src/routes/api/search/+server.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users can organize files into folders they own'
    - 'Users can search within their own data'
    - 'Users can manage their trash independently'
    - 'All user data remains private and secure'
  artifacts:
    - path: 'src/routes/api/folders/+server.ts'
      provides: 'Protected folder operations'
      contains: 'auth.getUser'
    - path: 'src/routes/api/tags/+server.ts'
      provides: 'Protected tag operations'
      contains: 'auth.getUser'
    - path: 'src/routes/api/trash/+server.ts'
      provides: 'Protected trash operations'
      contains: 'auth.getUser'
    - path: 'src/routes/api/search/+server.ts'
      provides: 'Protected search operations'
      contains: 'auth.getUser'
  key_links:
    - from: 'src/routes/api/folders/+server.ts'
      to: 'src/lib/auth.ts'
      via: 'auth.getUser()'
      pattern: "const.*=.*auth\\.getUser"
    - from: 'src/routes/api/search/+server.ts'
      to: 'src/lib/auth.ts'
      via: 'auth.getUser()'
      pattern: "const.*=.*auth\\.getUser"
---

<objective>
Add authentication guards to folders, tags, trash, and search API endpoints.

Purpose: Complete API security by protecting all data operations with user authentication.
Output: All API routes fully protected with authentication.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
@docs/PHASE2_API_CONTRACT.md
@src/routes/api/folders/+server.ts
@src/routes/api/tags/+server.ts
@src/routes/api/trash/+server.ts
@src/routes/api/search/+server.ts
</context>

<tasks>

<task type="auto">
  <name>Add auth guard to folders API</name>
  <files>src/routes/api/folders/+server.ts</files>
  <action>Update folders API with authentication:
- Add auth.getUser() to all folder operations
- Replace hardcoded user references
- Ensure folder operations scoped to user
- Update error responses for auth failures</action>
  <verify>Folders API requires authentication</verify>
  <done>Folders API protected with auth</done>
</task>

<task type="auto">
  <name>Add auth guard to tags API</name>
  <files>src/routes/api/tags/+server.ts</files>
  <action>Update tags API with authentication:
- Import and use auth.getUser()
- Scope tag operations to authenticated user
- Replace 'user_1' references
- Update response handling</action>
  <verify>Tags API requires authentication</verify>
  <done>Tags API protected with auth</done>
</task>

<task type="auto">
  <name>Add auth guard to trash API</name>
  <files>src/routes/api/trash/+server.ts</files>
  <action>Update trash API with authentication:
- Add auth checks to trash operations
- Ensure users can only access their own trash
- Update restore and empty operations</action>
  <verify>Trash API requires authentication</verify>
  <done>Trash API protected with auth</done>
</task>

<task type="auto">
  <name>Add auth guard to search API</name>
  <files>src/routes/api/search/+server.ts</files>
  <action>Update search API with authentication:
- Add auth.getUser() check
- Scope search results to user's data
- Update search query logic</action>
  <verify>Search API requires authentication</verify>
  <done>Search API protected with auth</done>
</task>

</tasks>

<verification>
Test API authentication:
- All API endpoints return 401 for unauthenticated requests
- Authenticated requests work with real user context
- User data properly isolated between users
- Error responses follow API contract format
</verification>

<success_criteria>

- All API endpoints require valid authentication
- Hardcoded 'user_1' references replaced with real user IDs
- Folder operations tied to authenticated users
- Search results scoped to user data
- Trash operations respect user permissions
- Clear 401 error responses for auth failures
- API responses follow documented contract
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-07-SUMMARY.md`
</output>
