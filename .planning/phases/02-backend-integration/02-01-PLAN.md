---
phase: 02-backend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wrangler.toml
  - cors.json
autonomous: false
user_setup:
  - service: cloudflare
    why: 'Cloudflare account and CLI access required for resource creation'
    env_vars: []
    account_setup:
      - task: 'Create Cloudflare account and install wrangler CLI'
        location: 'https://dash.cloudflare.com/'
    dashboard_config: []
must_haves:
  truths:
    - 'wrangler deploy succeeds without errors'
    - 'R2 bucket accepts file uploads (no 403 CORS errors)'
    - 'D1 database accessible from Workers'
    - 'KV namespace available for caching'
  artifacts:
    - path: 'wrangler.toml'
      provides: 'Cloudflare Workers configuration'
      contains: '[d1_databases], [r2_buckets], [kv_namespaces]'
    - path: 'cors.json'
      provides: 'R2 CORS policy for file uploads'
      contains: 'AllowedOrigins, AllowedMethods'
  key_links:
    - from: 'wrangler.toml'
      to: 'npm run cf:deploy'
      via: 'wrangler configuration'
      pattern: 'compatibility_date.*2026'
    - from: 'cors.json'
      to: 'R2 bucket'
      via: 'wrangler r2 bucket cors set'
      pattern: 'AllowedOrigins.*localhost:5173'
---

<objective>
Set up Cloudflare infrastructure (D1, R2, KV) with proper wrangler.toml configuration and R2 CORS policy for file uploads.

Purpose: Establish working Cloudflare deployment environment that supports file uploads and database operations.
Output: Configured Cloudflare resources ready for auth implementation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Cloudflare account setup and resource naming</decision>
  <context>Need Cloudflare account ID and preferred resource names for D1, R2, KV</context>
  <options>
    <option id="default-names">
      <name>Use default naming convention</name>
      <pros>Simple, predictable, follows research recommendations</pros>
      <cons>May conflict if multiple projects</cons>
    </option>
    <option id="custom-names">
      <name>Use custom project-specific names</name>
      <pros>Unique, clearly identifies CFS CMS resources</pros>
      <cons>Requires coordination across team</cons>
    </option>
  </options>
  <resume-signal>Select: default-names or custom-names, provide account ID</resume-signal>
</task>

<task type="auto">
  <name>Create wrangler.toml configuration</name>
  <files>wrangler.toml</files>
  <action>Create wrangler.toml with Cloudflare resources configuration:
- Set name to "cfs-cms"
- Configure compatibility_date for 2026
- Add D1 database binding for "cfs_cms"
- Add R2 bucket binding for "cfs-cms-files"
- Add KV namespace binding for "cfs-cms-cache"
- Include assets configuration for SvelteKit
- Use account_id from user decision</action>
  <verify>wrangler.toml exists and contains all required sections</verify>
  <done>wrangler.toml ready with Cloudflare resource bindings</done>
</task>

<task type="auto">
  <name>Create R2 CORS policy file</name>
  <files>cors.json</files>
  <action>Create cors.json with CORS policy for file uploads:
- Allow origins: localhost:5173 and production domain
- Allow methods: GET, PUT, POST, DELETE
- Allow headers: Content-Type, Authorization
- Expose headers: ETag
- Max age: 3600 seconds
- Follow research recommendations from STACK.md</action>
  <verify>cors.json exists with proper JSON structure</verify>
  <done>R2 CORS policy file ready for bucket configuration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cloudflare infrastructure setup (wrangler.toml, CORS policy)</what-built>
  <how-to-verify>
    1. Run `npx wrangler whoami` to verify CLI authentication
    2. Run `npm run cf:deploy` to test deployment
    3. Check Cloudflare dashboard for created resources
    4. Verify no deployment errors in terminal output
  </how-to-verify>
  <resume-signal>Type "verified" if deployment succeeds, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Test Cloudflare deployment pipeline:
- wrangler.toml syntax valid
- All bindings configured
- Deployment succeeds without errors
- Resources created in Cloudflare dashboard
</verification>

<success_criteria>

- wrangler.toml contains proper Cloudflare configuration
- R2 CORS policy file created for file uploads
- Cloudflare account authenticated with wrangler CLI
- npm run cf:deploy succeeds
- D1, R2, and KV resources created in Cloudflare account
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-01-SUMMARY.md`
</output>
