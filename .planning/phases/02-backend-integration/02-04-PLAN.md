---
phase: 02-backend-integration
plan: 04
type: execute
wave: 2
depends_on: ['02']
files_modified:
  - src/routes/api/workspaces/+server.ts
  - src/routes/api/files/+server.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - 'Users must log in to access their files and workspaces'
    - 'Each user sees only their own data'
    - 'Unauthorized access attempts are blocked'
  artifacts:
    - path: 'src/routes/api/workspaces/+server.ts'
      provides: 'Protected workspace CRUD operations'
      contains: 'auth.getUser'
    - path: 'src/routes/api/files/+server.ts'
      provides: 'Protected file operations'
      contains: 'auth.getUser'
  key_links:
    - from: 'src/routes/api/workspaces/+server.ts'
      to: 'src/lib/auth.ts'
      via: 'auth.getUser()'
      pattern: "const.*=.*auth\\.getUser"
    - from: 'src/routes/api/files/+server.ts'
      to: 'src/lib/auth.ts'
      via: 'auth.getUser()'
      pattern: "const.*=.*auth\\.getUser"
---

<objective>
Add authentication guards to workspaces and files API endpoints.

Purpose: Secure core workspace and file operations with user authentication.
Output: Workspaces and files APIs protected with authentication.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE2_ROADMAP.md
@.planning/phases/02-backend-integration/02-CONTEXT.md
@docs/PHASE2_API_CONTRACT.md
@src/routes/api/workspaces/+server.ts
@src/routes/api/files/+server.ts
</context>

<tasks>

<task type="auto">
  <name>Add auth guard to workspaces API</name>
  <files>src/routes/api/workspaces/+server.ts</files>
  <action>Update workspaces API with authentication:
- Import auth from lib/auth.ts
- Add auth.getUser() check in all handlers
- Replace hardcoded 'user_1' with real user ID
- Return 401 for unauthenticated requests
- Update error responses to follow API contract</action>
  <verify>Workspaces API requires authentication</verify>
  <done>Workspaces API protected with auth</done>
</task>

<task type="auto">
  <name>Add auth guard to files API</name>
  <files>src/routes/api/files/+server.ts</files>
  <action>Update files API with authentication:
- Import auth and get user context
- Add auth checks to all file operations
- Replace 'user_1' with authenticated user ID
- Ensure file operations respect user permissions
- Update upload endpoints with auth validation</action>
  <verify>Files API requires authentication</verify>
  <done>Files API protected with auth</done>
</task>

</tasks>

<verification>
Test API authentication:
- Workspaces and files APIs return 401 for unauthenticated requests
- Authenticated requests work with real user context
- User data properly isolated between users
- Error responses follow API contract format
</verification>

<success_criteria>

- Workspaces and files APIs require valid authentication
- Hardcoded 'user_1' references replaced with real user IDs
- Workspace operations tied to authenticated users
- File operations respect user permissions
- Clear 401 error responses for auth failures
- API responses follow documented contract
  </success_criteria>

<output>
After completion, create `.planning/phases/02-backend-integration/02-04-SUMMARY.md`
</output>
